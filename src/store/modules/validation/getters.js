export default {
  nameRegex(state) {
    return state.regex.name;
  },
  phoneRegex(state) {
    return state.regex.phone;
  },
  emailRegex(state) {
    return state.regex.email;
  },
  passwordRegex(state) {
    return state.regex.password;
  },
};
